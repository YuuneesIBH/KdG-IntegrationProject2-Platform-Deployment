# Secret voor gevoelige configuratie
# BELANGRIJK: In productie gebruik je externe secrets management (Google Secret Manager)
# Encode waarden met: echo -n 'waarde' | base64
apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: bordspelplatform
type: Opaque
data:
  # Database credentials (voorbeeld - verander deze!)
  DB_USERNAME: ZGJhZG1pbg==  # dbadmin
  DB_PASSWORD: Y2hhbmdlbWU=  # changeme
  
  # RabbitMQ credentials
  RABBITMQ_USERNAME: Z3Vlc3Q=  # guest
  RABBITMQ_PASSWORD: Z3Vlc3Q=  # guest
  
  # Keycloak admin credentials
  KEYCLOAK_ADMIN_USERNAME: YWRtaW4=  # admin
  KEYCLOAK_ADMIN_PASSWORD: YWRtaW4xMjM=  # admin123
